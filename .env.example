# ── AI API Keys ───────────────────────────────────────────────────────────────
# Groq (Llama) — used by ai_chart_scanner.py for AI-powered signal scanning
GROQ_API_KEY=

# OpenAI (GPT-4o-mini) — used by llama_analyzer_openai.py for Tier-1 validation
OPENAI_API_KEY=

# ── ProjectX / TopStepX Credentials ──────────────────────────────────────────
# Same as your TopStepX login credentials — used by data_feed.py for live futures data
PROJECTX_USERNAME=
PROJECTX_API_KEY=

# ── Optional: Local Ollama (fallback if no OpenAI key) ────────────────────────
# OLLAMA_MODEL=llama3
# OLLAMA_HOST=http://localhost:11434
